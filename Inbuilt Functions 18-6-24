#String functions in SQL are a set of built-in functions that allow you to manipulate and operate on string data. These functions provide various operations such as :
#1.CHAR_LENGTH(str): This function returns the length of the given string str in characters.
#example
SELECT CHAR_LENGTH('Hello, World!');

#2.CONCAT(str1, str2, ...): This function concatenates two or more string values together.
#example
SELECT CONCAT('Hello', ' ', 'World');

#3.  TRIM(str), RTRIM(str), LTRIM(str): These functions remove leading and/or trailing spaces from the string str.
--    TRIM removes leading and trailing spaces, RTRIM removes trailing spaces, and LTRIM removes leading spaces.
#EXAMPLE
SELECT TRIM('   Hello, World!   ');
SELECT RTRIM('   Hello, World!   ');
SELECT LTRIM('   Hello, World!   ');

#4. SUBSTR()
#Example:Display First Three Characters extracted from jobs of employees 8888 and 8900
SELECT SUBSTR(job,1,3)
From empl
WHERE empno=8888 or empno=8900

#5. INSTR(str, substr): This function returns the position of the first occurrence of the substring substr in the string str.
#Example
SELECT INSTR('Hello, World!', 'o');

#Date and Time Functions
#1.CURDATE()
#EXAMPLE
SELECT CURDATE()

#2. MONTH()
#EXAMPLE
SELECT MONTH('2020-02-03')

#3. NOW()
#EXAMPLE
SELECT NOW()


#4.SYSDATE()
#EXAMPLE
SELECT SYSDATE()


# Numeric Functions
#1. AVG()
#EXAMPLE
SELECT AVG(price) AS avg_price
FROM products;

#2. COUNT()
#EXAMPLE
SELECT COUNT(*) "TOTAL"
FROM empl;

#3. POW()
#EXAMPLE
SELECT POW(5, 3) AS result;

#4. MIN()
#EXAMPLE
SELECT MIN(price) AS min_price
FROM products;

#5.MIN()
#EXAMPLE
SELECT MAX(stock) AS max_stock
FROM products;

#6. ROUND()
#EXAMPLE
SELECT ROUND(3.1416, 2) AS result; 

#7. SQRT()
#EXAMPLE
SELECT SQRT(25) AS result; 





